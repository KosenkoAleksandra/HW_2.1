package transport;

public class Bus extends Transport {
    public Bus(String brand, String model, String color, int year, String country, String maxSpeed) {
        super(brand, model, color, year, country, maxSpeed);
    }
}
